---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { useTranslations, useTranslatedPath } from '../../utils/i18n';
import PageHeader from '../../components/PageHeader';
import TourGrid from '../../components/TourGrid';

import BookingCTA from '../../components/BookingCTA.astro';
export function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'es' } }
  ];
}

const { lang: rawLang } = Astro.params;
const lang = rawLang as 'en' | 'es';
const t = useTranslations(lang);
const translatePath = useTranslatedPath(lang);

const tours = await getCollection('tours');
const dailyTours = tours
  .map(t => t.data)
  .filter(t => t.category === 'tour-days');
---

<Layout title={`${t('nav.daily')} | Machu Picchu Andes Marathon`} lang={lang}>
  <PageHeader 
    client:load
    lang={lang}
    title={lang === 'en' ? 'Daily' : 'Tours de'}
    titleAccent={lang === 'en' ? 'Tours' : 'un Día'}
    subtitle={lang === 'en' ? "Short immersions, deep connections. Experience the highlights of Cusco and the Sacred Valley in a single day." : "Inmersiones cortas, conexiones profundas. Experimenta los puntos más destacados de Cusco y el Valle Sagrado en un solo día."}
    bgImage="/images/sacred-valley.webp"
  />

  <section class="section">
    <div class="container">
      <TourGrid client:visible lang={lang} tours={dailyTours} />
    </div>
  </section>

  <BookingCTA lang={lang} />
</Layout>


