---
import Layout from '../../layouts/Layout.astro';
import { Star, Clock, ArrowRight } from 'lucide-astro';
import { allTours } from '../../data/tours.js';
import { useTranslations, useTranslatedPath } from '../../utils/i18n';

export function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'es' } }
  ];
}

const { lang } = Astro.params;
const t = useTranslations(lang);
const translatePath = useTranslatedPath(lang);

const altTours = allTours.filter(t => t.category === 'alternative-treks');
---

<Layout title={`${t('nav.alternative')} | Machu Picchu Andes Marathon`} lang={lang}>
  <!-- Page Header -->
  <section class="page-header alt-bg">
    <div class="header-content container">
      <h1 class="serif animate-fade-in">{lang === 'en' ? 'Alternative' : 'Treks'} <br/> <span>{lang === 'en' ? 'Treks' : 'Alternativos'}</span></h1>
      <p class="animate-fade-in delay-1">{lang === 'en' ? "Beyond the beaten path. Discover the hidden spirits of the Andes through our curated alternative routes." : "Más allá de las rutas comunes. Descubre los espíritus ocultos de los Andes a través de nuestras rutas alternativas curadas."}</p>
    </div>
  </section>

  <!-- Tours Grid -->
  <section class="section">
    <div class="container">
      <div class="tour-grid">
        {altTours.map((tour, index) => (
          <div class={`tour-card animate-fade-in delay-${index % 3}`}>
            <div class="tour-image-w">
              <img src={tour.image} alt={tour[lang].title} />
              <span class="tour-tag">{tour[lang].tag}</span>
            </div>
            <div class="tour-body">
              <div class="tour-meta">
                <span><Clock size="16" /> {tour[lang].duration}</span>
                <span><Star size="16" fill="var(--secondary)" color="var(--secondary)" /> {tour.rating}</span>
              </div>
              <h3 class="serif">{tour[lang].title}</h3>
              <p class="tour-desc">{tour[lang].description}</p>
              <div class="tour-footer">
                <span class="tour-price">{t('tour.from')} {tour.price}</span>
                <a href={translatePath(`/tours/${tour.slug}`)} class="btn-icon"><ArrowRight size="20" /></a>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Booking CTA -->
  <section id="contact" class="cta-section container">
    <div class="cta-box animate-fade-in">
      <div class="cta-content">
        <h2 class="serif">{lang === 'en' ? 'Start Your Adventure' : 'Comienza tu Aventura'}</h2>
        <p>{lang === 'en' ? "Expert guides and premium equipment for the most challenging Andean trails." : "Guías expertos y equipo premium para los senderos andinos más desafiantes."}</p>
        <form class="booking-form">
          <input type="text" placeholder={t('form.name')} required />
          <input type="email" placeholder={t('form.email')} required />
          <select required>
            <option disabled selected>{lang === 'en' ? 'Select Trek' : 'Selecciona Trek'}</option>
            {altTours.map(tour => (
              <option>{tour[lang].title}</option>
            ))}
          </select>
          <button type="submit" class="btn btn-primary">{t('form.submit')}</button>
        </form>
      </div>
    </div>
  </section>
</Layout>

<style>
  .page-header {
    height: 60vh;
    min-height: 480px;
    background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('/images/hero.png') center/cover no-repeat;
    display: flex;
    align-items: center;
    color: white;
    padding-top: 120px;
  }

  .header-content {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }

  .page-header h1 {
    font-size: clamp(2rem, 6vw, 3.5rem);
    margin-bottom: 1.5rem;
  }

  .page-header h1 span {
    color: var(--secondary);
  }

  .page-header p {
    font-size: 1rem;
    max-width: 600px;
    opacity: 0.9;
  }

  @media (max-width: 768px) {
    .page-header {
      height: 50vh;
      text-align: center;
    }
    .header-content {
      margin: 0 auto;
    }
  }
</style>
